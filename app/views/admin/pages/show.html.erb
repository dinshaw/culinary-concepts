<%= render :partial => "shared/html_head_tags", :locals => {:page_item => @page} %>

<h2><%= @page.title %></h2>
<p><%= @page.description %></p>

<fieldset>
	<legend>Meta Tags</legend>
<p><%=h meta "description", meta_description %></p>
<p><%=h meta "keywords", meta_keywords %></p>
</fieldset>

<p><%= link_to 'Edit', edit_admin_page_path(@page) %> | <%= link_to 'Back', admin_pages_path %></p>
